using Poolgramming.State;

namespace Poolgramming.ImageProcessing
{
    public class TableStateProcessor
    {
        public static TableState GeTableState(byte[] image)
        {
            return new TableState();
        }
    }
}
